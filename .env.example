# Docker Compose runtime configuration. No secrets needed for the prototype.
# For pnpm dev: copy apps/web/.env.example to apps/web/.env.local instead.
APP_ENV=demo
EVERWOVEN_PORT=3100
# Leave unset to follow EVERWOVEN_PORT automatically.
# APP_ORIGIN=https://everwoven.example.com
