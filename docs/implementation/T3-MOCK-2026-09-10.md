# T3 / 前端 Mock 首轮实施记录

2026-09-10 · 当前切片完成代码与自动验证，完整业务接库/真实生成尚未完成。

## 已实施

- 在现有`apps/web`增量接入T3组合，未创建第二个工程或启动入口。
- 精确依赖：tRPC server/client/tanstack-react-query 11.18.0、TanStack Query 5.102.8、Zod 4.5.4。既有Next/React/Prisma版本保留。
- AppRouter + Zod输出契约 + Service + 同源React请求上下文；第一个操作仅为只读`video.configuration`。
- 原配置REST路由移除，既有调用点改为tRPC。localhost内部标准化与实际Host校验分别处理，保持固定本机origin、不开放CORS。
- 模拟内容集中到前端mocks；删除假模型VideoProvider，正常默认值/图片解析从demo-data分离；Mock种子返回副本、不初始化用户数据。
- 默认开始路径接入MockSession → 共用SegmentedStage：模拟等待、显式完成事件、情境建议/自由回应、收纳、画面适配、模拟失败/重试。
- 原库不清空/不切SQLite。Save的独立`mockSession`版本化命名空间保存阶段/轮次/草稿，和回应单次写入同一browser key。损坏数据拒绝覆盖；保存失败保留pending；旧记录无checkpoint则按已有回应数初始化。
- 已有原生草稿替换弹窗改为页面内确认；退出有保留/离开选择。草稿UI恢复有组件静态渲染与存储往返测试。
- README、产品交付说明、技术/API主入口统一指向最新T3裁决；旧静态设计源仅供追溯。结束检查仅3100监听，3103无服务。

## 验证

| 检查 | 本轮结果 |
|---|---|
| TDD | 观察到未实现router/mock/状态机及无舞台渲染的预期失败，再实现；HTTP真实冒烟暴露Next规范化Host差异，补失败回归后修复 |
| frozen install | 通过原供应链策略；Zod最初选到发布冷却期版本，改为4.5.4并移除自动生成的排除项；最终无新增policy例外 |
| 单元/集成/SSR | **18个文件、173项通过**，含既有SQLite集成切片；不是173项产品功能验收 |
| 类型检查 | runtime源码/测试与Web全部通过 |
| 构建 | runtime + Next生产构建通过 |
| 真实HTTP | 本机`video.configuration`单请求和batch返回200；外部Origin返回403；无敏感配置输出 |
| 浏览器已走通 | 同一应用打开示例→演练说明→模拟等待→观看态无输入→显式结束→建议→收起/恢复→选择推进下一轮；截图确认单舞台铺满。原2个用户草稿及历史记录仍可见 |
| 浏览器未完成项 | 草稿替换验收中旧原生confirm阻塞浏览器自动化，取消/关闭操作未成功；已改页面内确认，但最终替换取消、重新打开恢复及失败重试的完整浏览器复测未计通过。源码、存储往返/恢复/SSR测试和独立只读复核通过不替代该项 |

新增测试覆盖：Mock不读写用户种子/不请求网络、拷贝隔离、阶段门禁、重复回应与旧完成事件、模拟重试、checkpoint版本/轮数/损坏拒绝、草稿呈现、不暴露写入/收费操作、来源校验、tRPC真实HTTP格式、只读配置不泄露凭证/路径。

浏览器本轮新增一条明确演练记录并用于回应测试，未修改既有用户剧本/角色/图片。临时验收标签遇到的原生确认若仍存在，需要在浏览器内取消；新代码不再使用原生确认处理该交互。

## 下一切片

本机启动身份/受保护tRPC → 复用Prisma/WriteGate → 剧本create/get/update/delete → SQLite保存重开，随后人物和图片。正式SQLite CRUD、身份、预算、MiniMax生成与有效播放/事实提交、本机备份和Tauri均未在本轮声称完成。未发生付费模型调用。

[最新架构、时序与API迁移裁决](../architecture/T3-FRONTEND-MOCK-2026-09-10.md)。
