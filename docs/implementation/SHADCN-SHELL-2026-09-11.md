# 未完 首版工程整理：成熟弹窗、分区布局与 Web 部署

2026-09-11 · 本轮实施记录，不是正式视频生成验收。

## 1. 已落地

- 用户最终选择恢复产品名「未完」及原 w. 标识，撤回未发布的 Mira 品牌方案；包名 weiwan，版本 0.1.0；更新页面标题、侧栏、主 README 和当前产品/技术文档标题。保留旧浏览器存储键与历史文档代号，未搬动运行目录。
- 引入 shadcn 官方 registry 源码改编的 AlertDialog/Button，Radix 作为单一底层；保留 MIT 与本地修改说明。主题使用局部 CSS Modules 接入晴空蓝紫，不全站执行覆盖式初始化。
- StageConfirmation 仅处理业务结果、草稿与明确焦点目的地；模态隔离/焦点圈定交给 Radix。专用 portal 宿主在舞台内，null 时不偷挂 body，支持浏览器系统全屏。
- AppShell / AppSidebar 独立组件：左边品牌/底部区域局部固定，中间导航独立滚动；右边头部固定，内容滚动。窄窗口折叠图标轨道，短窗口隐藏装饰说明，不把定位逻辑散落进业务页面。
- Next standalone 多阶段 Dockerfile、Compose、默认非 root/loopback、部署文档与 GitHub CI。APP_ORIGIN 是明确部署配置，不从转发头自动推断可信来源；自定义端口默认值同步。
- Next typecheck 先执行官方 next typegen，避免首次拉取时依赖本机 .next 产物；生成的 next-env.d.ts 不提交。

## 2. 测试与走查

### 主仓重新执行

`pnpm test && pnpm typecheck && pnpm build` 全部退出 0：**24 个测试文件，241 项通过**；runtime/Web 类型检查与生产构建通过。

新增/替换覆盖：

- AlertDialog 可访问名称、舞台 portal、安全默认焦点、反向 Tab 圈定、Escape 不冒泡、明确确认一次、取消保留草稿、StrictMode 清理与失败后草稿/焦点。
- AppShell 分区结构、当前导航与业务回调委托。
- 部署来源：公开 HTTPS Host、转发头不构成信任、无效来源配置。

先出现旧组件缺少新语义/portal行为的失败，再替换实现；布局测试也先由占位实现失败，再落地。DOM 测试仍不替代浏览器验证。

### 独立 Chromium 会话

使用现有 3100 应用，隔离测试会话，不清空用户浏览器内容：

1. 剧本广场 → 示例详情 → 开始前端演练 → 模拟片段结束 → 自由草稿。
2. 进入系统全屏后修改建议，断言 alertdialog 在 fullscreenElement 内；默认焦点为保留原稿，反向 Tab 不越界，背景点击不丢稿。
3. 取消后焦点回到发起按钮，原草稿仍在；确认后替换文本并聚焦输入框，不隐式发送。
4. 普通窗口 Escape 只取消确认框，不把回应面板一起收起。
5. 1440×900、1024×480、390×844 检查侧栏/主区，主内容滚动不带动品牌和底部，页面与主区无横向溢出。截图只留本地，不随源码提交。

失败后输入保护由 DOM 测试覆盖；未在这轮浏览器走查中模拟真实供应商失败、Safari、输入法组合事件或屏幕阅读器，不能据此声称跨浏览器全量验收。

### 部署验证

- Compose 默认值解析通过；WEIWAN_PORT=3200 时端口和 APP_ORIGIN 同步；显式 HTTPS APP_ORIGIN 优先。
- 主仓 standalone 产物在临时测试端口启动，首页、静态参考图片与源文件一致、配置元数据 unselected、异源 403 均通过，测试进程已停止。
- 本机 Docker daemon 报错，镜像运行尚待 GitHub CI；不以 standalone 进程测试代替 Linux 容器验证。

## 3. 独立审查

只读组件审查未发现所查范围内明确 P1/P2。部署审查发现自定义端口与来源默认值不同步（P2），已修正为同一端口回退并在 Compose 解析与 CI 双端口冒烟中覆盖，复核关闭。源码发布审查要求保留 Prisma SQL 和必要参考图片、剔除私有附件/数据库/密钥、保留第三方授权，已纳入提交检查。

## 4. 仍未完成

官方指定模型真实生成、付费测试、浏览器创作数据迁入 SQLite、身份与受保护业务 API、分支存档 UI、AI 创作 Chat/画布、Tauri 安装包均未因本轮整理变为完成。当前只提供同一 Web 原型，不建立第二套站点或假持久化数据卷。

下一切片仍是本机身份/数据保护与真实创作 CRUD 接入；正式付费链路需独立预算和验收。

## 5. 后续环境配置要求（待实施）

用户要求通过 `APP_ENV=demo|dev|prod` 选择环境，便于同一 Docker 镜像运行；该变量与 Next 的 NODE_ENV 分离。当前尚未实现三环境切换，现有默认仍为前端 Mock。环境配置落地及验证完成前暂缓首版 GitHub 推送。

## 6. 仓库命名确认

用户后续确定 `everwoven` 为仓库与工程标识，中文产品名仍为「未完」；Docker 镜像统一 `everwoven-web`，Compose 项目名 `everwoven`，自定义宿主端口变量为 `EVERWOVEN_PORT`。上文 weiwan/Mira 是本轮历史变更记录，不再是当前交付标识。浏览器历史存储键、本地目录、数据库文件均不随仓库改名迁移。

三版 Logo 为概念提案，尚未确认采用哪版，不因本次仓库命名自动更换界面标识。`APP_ENV=demo|dev|prod` 实施仍待完成，不能仅凭 Docker 打包认定环境切换已生效。

## 7. 环境切片后续实现

用户授权继续到 GitHub 首版交付，APP_ENV 已落地；§5/§6 的待实施状态是历史记录，以[环境配置](../deployment/ENVIRONMENTS.md)及最新验证为准。默认 demo 保留演练；dev/prod 保留创作且关闭假游玩；localStorage/IndexedDB 按环境分区，不迁移或删除旧 demo 数据。Page 使用 connection() 读取运行时枚举，同一个构建产物已分别启动三环境验证成功。
