# M1-C1 · 私有图片实施记录

## C1a 边界与真实解码

单一实现者Feynman已停止写入。写集为runtime资产契约/校验、ImageNormalizer端口、容器/预算/sharp适配器、三组测试和精确直接sharp0.35.4依赖；无HTTP/DB/fs/UI变更，无用户图、凭据或数据重置。

实现者报告的TDD证据（不代替主仓实跑）：
- 严格契约首次66失败；decoder/预算35失败、1通过；公开package导出回归1失败。
- 聚焦3文件107/107 GREEN；runtime build、源码和测试typecheck、真实Node compiled-dist规范化成功。
- 原生timeout非进程强杀、无OS级RSS硬限；超时后实际作业不结束就继续占槽，没有释放容量制造假取消。

主会话已启动全量、双端typecheck及隔离生产构建/三项Chrome，Hegel解码规格、Dewey契约规格复核中；尚未计最终通过。详尽字段见[资产契约](../api/ASSET-CONTRACT-M1-C1.md)，后续文件/事务/HTTP及原控件仍待实现。


## 主仓及独立复核证据

- 主仓`pnpm --filter runtime build && pnpm exec vitest run --maxWorkers=1 && pnpm typecheck`：**58文件853/853、runtime/Web类型检查通过，退出0**。
- 隔离Node22 offline frozen安装、生产构建、HTTP演练/原角色/原root三项Chrome smoke全部通过；仍未调用任何模型。
- 主仓直接运行compiled-dist normalizer，合成JPEG/PNG/WebP三格式分别转静态320×512 WebP，真实输出decode/hash/byteSize断言通过。
- Dewey契约规格独立67/67 PASS；Hegel解码规格独立107/107 PASS，另compiled-dist及metadata延迟注入通过：两个任务超时后第三个仍busy，实际任务结束容量才恢复。
- Hegel核对当前sharp原生源码：metadata worker不使用pipeline timeout，前移`.timeout()`并不提供metadata硬取消；当前等待完成、检查deadline、占槽直到结束的语义符合设计。槽限于当前进程该模块，不作为跨进程总预算。
- Cicero最终代码质量审查进行中；暂不提交C1a。

最终Cicero代码质量审查PASS，无确认P1/P2；本切片可提交。真实文件/上传HTTP/角色图片端到端仍需下一C1b/C1c/C2实现和验收。

C1a已提交推送88f5940，远程CI34716208568执行中。C1b私有文件端口已派Feynman按计划实施；源码尚未验收，不记为已完成。

88f5940远程CI34716208568最终成功（verify4m40s），含Linux测试/生产构建/Docker六模式端口和三项浏览器smoke；非tag未发布镜像。C1b仍由Feynman实施，未计完成。


## C1b 实施与首次审查

Feynman停止写入，报告初始RED41失败、补强2失败，聚焦161/161及runtimebuild/typecheck通过；新增私有文件/目录/删除许可/cleanup锁/输出验证器，normalizer与verifier共享2槽。无HTTP/DB/UI接入。主仓全量60文件907/907通过，类型检查结果另记。

尚未标验收：主会话发现O_EXCL cleanup锁进程崩溃后会永久busy，而显式停机恢复工具尚不存在。Hegel正在评估残锁恢复与更小的SQLite协调端口取舍；不能仅因fail-closed就把后续cleanup重试称为已闭环。Dewey并行核对输出验证/共享预算/惰性工厂衔接。

两项修订后Feynman报告RED13失败/1通过→177/177；主仓独立全量**61文件923/923及runtime/Web类型检查通过（exit0）**。Dewey端口衔接复核PASS；Hegel文件最终复核中，隔离生产构建/三Chrome仍在执行。尚未提交C1b。

Hegel第二次规格复核关闭原残锁/持久化两项，但新增复现两个P2：包装cleanup hook返回rejected Promise被抛错后丢弃，Node22仍因unhandled rejection退出1；非法UUID测试用v7().replace('-7','-4')可能修改时间字段仍是合法v7，发生实际偶发失败。已派Feynman补进程级回归/消费拒绝但不await并修私有fixture；Dewey独立修相同问题的角色和dataset契约两测试文件。当前923/923和生产三Chrome通过不覆盖额外复现，C1b尚未通过最终门槛。


## C1b最终本地验收

- 最后修订实现者报告RED4失败/40通过→180/180；Dewey两fixture修复RED4失败/42通过→46/46。主仓统一复跑**62文件928/928、runtime/Web类型检查，exit0**。
- 最终隔离Node22生产构建+HTTP演练/原角色/原root三Chrome成功。
- 两个独立Node进程在真实初始化的临时Host上验证compiled文件端口：第一个无覆盖durable写入后退出；第二个确认同dataset，读回实际hash/metadata并ensureDurable成功。未调用cleanup，测试注入的协调器只抛“未纳入本测试”，不拿它冒充生产互斥。临时目录已清理。
- Hegel最后规格复核3文件64/64，四项P2全部关闭；Dewey预算/输出/端口规格通过；Cicero最终只读质量PASS。
- 没有永久cleanup锁，没有默认进程内协调fallback；实际SQLite协调、跨进程SIGKILL/超时属于C1c硬门槛。Linux结果待新CI，不提前标验证。

本批提交主题`feat(assets): add durable private file ports and cleanup contract`；无业务库重置，无用户图片/密钥操作，无付费模型或发布tag。
