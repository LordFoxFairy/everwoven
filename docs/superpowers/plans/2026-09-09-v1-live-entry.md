# 一期：统一实时启动与会话生命周期

依据已批准的一期路线，本次落实端到端启动/退出切片，不实现二期热点。

- [ ] 用失败测试覆盖启动防重、未就绪输入、连接中退出、生成回执不冒充画面呈现、拒绝输入草稿恢复。
- [ ] 建立独立 SessionController，区分连接状态、播放状态和意图状态；保留片段元数据。
- [ ] Platform 直接进入 LiveSession，正式入口不再经过静态 Player；修正免费模拟文案。
- [ ] 准备页说明真实能力与费用；点击开始后自动发出开场配置。播放区点击不唤出输入。
- [ ] 改善连接等待、明确结束、退出草稿确认、自动播放恢复、重试边界。
- [ ] 类型/单测/构建以及无密钥浏览器流程验证。

后续独立验收：真实 API 至少三次方向改变，实际响应时间与计费关闭；叙事运行时及剧情触发卡片。无真实证据时不制造卡片或声称一期全部完成。
