# 情境触发游玩页调整

依据用户批准的方向，撤销上一版常驻侧栏。

已实现：默认画面；一次性操作提示自动消失；点击画面主动表达；Esc 收起输入或打开系统菜单；记录和退出收纳；没有情境事件就没有剧情选项。鼠标悬停/键盘焦点可发现系统菜单，触屏保留小菜单入口。静态预览标识保留。

验证：类型检查、9项现有测试、生产构建通过。浏览器检查已有存档进入、主动展开表达、Esc收起；未提交测试内容，不改用户存档。

尚未实现：模型会话、剧情事件驱动卡片、视频响应。此次未用计时器或本地记录伪造剧情触发。
