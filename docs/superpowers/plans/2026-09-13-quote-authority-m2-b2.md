# M2-B2 — 首幕报价的宿主authority与费用包络

上一goal轮是实码progress：753c928固定三阶段Profile并完成1863/生产验证，精确CI34747523380仍live，继续同一作业。

1. 持久storeEpoch独立宿主状态；新host发布ready前创建，已有host显式受控登记，普通读不修复或轮换。所有权/dataset/文件身份/恢复标记/维护锁均检查；authority可重新校验，先写测试。
2. 首幕Quote复用封存preparing事实和固定video binding；补唯一startup快照中的执行配置选择及窄能力/价格/artifact证据端口。未知价格/能力阻断，不写0。
3. 一张Quote表+同Gate幂等回执，完整授权包络/可读摘要；整数有理数上界，过期回放不续期。不新增管理页。
4. 接原准备浮层费用确认后，紧接BudgetScope/两级预算/TurnRun/Reservation/Outbox原子接受，再Worker和媒体；本轮缺付费批准不调用模型。

storeEpoch不是datasetId/writeEpoch/dispatchEpoch。恢复协议暂只提供隔离门，完成恢复必须由后续真实备份恢复流程同时废止许可/事件流等，不提供一个裸clear-marker伪恢复按钮。停止旧进程是离线维护前提，FS与DB不是单一ACID事务。

## 用户2026-09-14纠偏

暂停新增抽象与逐片扩张管理能力。后续以一次可跑的纵向切片组织：原开始按钮→有依据的费用确认→原子接受/任务记录→官方创建和恢复查询→私有视频播放器→播后选择/自由回应→第二幕与刷新续玩。使用现有tRPC/Prisma和能力目录，普通轮询即可；不为每一小环节新建框架或先做全能工作流平台。现有维护保护保留，但不再让更多非主线门槛拖延可玩流程。未取得付费授权不调用真实模型。
